export const getVolumePorCliente = async (req, res) => {
  res.json({ message: "Volume por cliente" });
};

export const getVolumePorColaborador = async (req, res) => {
  res.json({ message: "Volume por cliente" });
};